{
  "_note": "ghidra runs as a shared HTTP daemon (systemd --user ghidra-openrecet.service on :8200) so concurrent Claude sessions don't fight over the single-writer .gpr lock. See slopstudio docs/INFRA.md. Start/status: systemctl --user status ghidra-openrecet",
  "mcpServers": {
    "openrecet": {
      "type": "stdio",
      "command": "/home/headpats/.local/state/openrecet/mcp-python/bin/python3",
      "args": ["/opt/src/openrecet/tools/openrecet_mcp.py"],
      "env": {}
    },
    "ghidra": {
      "type": "http",
      "url": "http://127.0.0.1:8200/mcp"
    }
  }
}
