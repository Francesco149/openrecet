{
 "_note": "RUNTIME-axis source for the port ledger (EP-06). Git-tracked, hashes/scopes ONLY (no proprietary bytes) — the reproducible binding of an engine VA to a parity-proof CONTRACT. DURABLE KEY = contract_sha256 (parity_prove.contract_sha256: the stable hash of the scenario's `proof` block; drive- and commit-INDEPENDENT — it reproduces from the committed scenario.yaml, so it can be cited inside the very commit that adds the entry). `proof_id` is OPTIONAL + ADVISORY: a recording-time snapshot of the LOCAL bundle that established the binding (it binds git_commit + drive and advances every commit; runs/proofs/ is gitignored) — a human locator, never the identity. The generator NEVER reads runs/proofs/. A curator adds an entry only when a parity_prove.py bundle actually covers the VA in its proven scope (a PASS on the required pillars); the generator then advances that VA's runtime_state. Empty entries == 0 functions runtime-proven. See docs/findings/parity-EP06-ledger-lifecycle.md and docs/findings/parity-save-producer.md (★NEXT-b′, the first entry).",
 "schema_version": 1,
 "runtime_states": [
  "retail-executed",
  "port-executed",
  "call-I/O-aligned",
  "scenario-pillar-proven",
  "matrix-proven"
 ],
 "entries": [
  {
   "va": "0x4905a8",
   "state": "scenario-pillar-proven",
   "contract_sha256": "c8c9a6a528a1ba555e7b41d482175c2c6809e79b428c2210065fe86ed1b9b4b0",
   "scenario": "house-pause-save-commit",
   "scope": "save-commit path rooted at FUN_004905a8 (save_io_commit_slot): over house-pause-save-commit win-0-200 @SAVE_PICKER_READY[1,19] the committed save.dat is byte-identical to retail AND the once-per-frame VOLATILE state (rng/phase, the save-picker's deterministic LCG) is Merkle-identical port↔retail (19/19; the full 200-frame window 200/200). parity_prove verdict PASS (required pillars identity+save+state, 0 divergences; render_program PASS bonus) — the first three-pillar (identity·save·state) volatile+persistent proof. Normalized origins: {rngseed 19937}, {phasepin}, load pins, and the bilateral {playtimepin} (playtime-at-commit is a load-timing phase origin, not the logic contract).",
   "pillars": ["identity", "save", "state"],
   "proof_id": "7ab74d228b07764dff920333f188def0a5d557f72309d8cdfe0cb62e09dc670e"
  }
 ]
}
