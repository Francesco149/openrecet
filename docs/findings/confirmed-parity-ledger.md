# Confirmed parity ledger — what is (and is NOT) 1:1 with retail

> **Purpose (user directive, 2026-06-01):** keep an explicit, durable record of
> which behaviours have been **confirmed with human eyes** to match retail (or
> not), so we never re-litigate a settled fact, and never hand-wave a real
> divergence away as "jitter/RNG/phase" until it is **100% human-confirmed**.
>
> **Rules:**
> - A row is **CONFIRMED 1:1** only when the *user* has eyeballed a zoomed
>   port-vs-retail diff (on a deterministic TAS trace) and said so.
> - A row is **CONFIRMED NOT 1:1** when the user has flagged it as wrong/off.
> - Anything else is **UNVERIFIED** — do not assert it matches, and do not
>   dismiss a divergence in it as benign, until a human confirms.
> - Update this file the moment the user confirms/denies something. Cite the
>   feed push id / scenario / date.
> - **DEFERRED ≠ benign ≠ resolved.** When the user says a small divergence
>   "isn't worth chasing right now," it goes in the **Deferred** table below and
>   stays there until fixed — it is a known real gap, not something to forget or
>   later hand-wave away.

## DEFERRED (real divergences the user chose not to chase yet — keep tracking)

| subsystem | divergence | deferred when / why | revisit trigger |
|---|---|---|---|
| Recette **idle** animation phase | idle pose "slightly off" on `house-movement` **cap_00 (first frame = idle free-roam)**; she's **1:1 while moving** (cap_01/02). Likely idle-phase accuracy, not position. | user, 2026-06-01 ("probably accuracy issues on idle phase that we'll chase later") | idle-animation faithfulness pass; isolate idle-phase counter vs retail |
| ~~Tear (companion) appearance~~ — **RESOLVED → 1:1 2026-06-05** | was RNG-phase noise, not position/anim/render — see the RESOLVED row in the NOT-1:1 table below + CONFIRMED-1:1. `phase_probe house-idle-npc-drift` = PHASE-CLEAN; full frame bit-1:1 once `{rngseed}` is added. | resolved |
| Foot-dust position/phase | RNG-stream desync vs retail | user: visible bugs first | after the visible free-roam render gaps close |
| Background-window NPC anim/identity phase (`scene1_bg_npc`, was "ambient motes") | sprites now RENDER + animate (user-verified 2026-06-02); exact per-frame char/anim-phase vs retail not yet pinned | sprites landed 2026-06-02; phase a faithfulness follow-up | bg-npc anim-phase pass; see [[scene1-bg-npc]] |
| Suspected real ambient particle (the "tiny dots") | a few tiny dots don't match in diffs; very faint | was wrongly attributed to "ambient motes" (that's the NPC system, now resolved); a genuine faint ambient effect likely exists but is unfound | when chasing free-roam visual parity; find the real effect, don't re-conflate with bg-npc |
| ~~Wing-flap phase alignment~~ — **RESOLVED → 1:1 2026-06-05** | the companion anim counters were already machine-verified bit-exact (2026-06-04); 2026-06-05 the full pinned pixel-diff confirmed the *drawn* wings (glow + sparkle particles) are 1:1 too once phase **and** RNG are pinned (`phase_probe` adds both). The flap "phase noise" was the same RNG-origin issue as the rest of Tear — see the RESOLVED Tear row below. The `{phasepin}` normalizes the load-dependent origin for comparisons; the shipped game keeps the engine-faithful free-running counter (retail's is equally load-dependent). | resolved 2026-06-05 |
| Guild menu **hand-cursor bob** + **"New" badge sparkle** phase | at the resting menu (label 02191) the cursor bob is ~3px off and the "New" sparkle color is a sub-pixel phase off vs retail (position + mean color match within 1). Both are the same load-seam phase residual — the menu resumes a few frames apart on port/retail (the bob `g_anim_counter` + the sparkle's `c38` origin differ by the kept-count seam). | user, 2026-06-11 ("slight phase desync on cursor and 'new' badge that we'll fix after we get the rest of the menu ported") | after the rest of the guild menu (nav/buy flow) is ported; pin the cursor-bob + entry-tick origin across the guild load seam (same class as the kept-count seam) |

## CONFIRMED 1:1 (human-verified)

| subsystem | scope | evidence | date |
|---|---|---|---|
| **Pause menu BACKDROP [0] + radial-blur composite (M3, mode 9)** | ESC in-game → the captured-screen render target: the live scene re-renders into RT#56, a 2-pass radial-blur composites it, and it's sampled full-screen as the menu backdrop with the `c99c·0x16` fade-in ramp; the option list (Items·Encyclopedia·Options·Save·Exit) + "PAUSE MENU" header on top. | **user confirmed** in-game "the pause menu looks correct on port" + in the v3 viewer "confirmed 1:1 when settled" (the resting menu) and the open animation "eyeballing it it looks 1:1", 2026-06-13, `house-pause` window 150:90. Machine backing: port self-verify `replay.exe --verify-hashes` = **240/240 BIT-EXACT**; the RT command program matches retail's f40/41 (capture + 2-pass blur, every clear+prim-count); blur frame f41 gt8 0.46% vs retail; resting f119 right-half (option list) matches. `a8dd8f6`. See `plans/pause-menu.md` M3. **Scope = the backdrop + resting menu + the fade-in animation LOGIC.** The open-animation TIMING can't be frame-confirmed — the port's async pause-asset load shifts it vs retail (a "long gap then late animation"); the load-seam phase pillar (DEFERRED, same class as the guild menus). **PORT-DEBT:** calendar/numbers [4-9] → now CONFIRMED 1:1 (M2c, next row); submenus / exit-confirm / unpause-restore (M3+). | 2026-06-13 |
| **Pause menu CALENDAR + numbers [4-9] (M2c, mode 9)** | the resting pause-menu calendar board + today/period-end day markers + the merchant-rank XP progress bar + the gold/quota/level number glyphs (FUN_004820ba [4-9] + helpers FUN_00482033/59/d997, reading the working save bank directly). | **user confirmed** "can confirm pause menu is fully 1:1 based on the pushes", 2026-06-13 (feed "M2c pause calendar+numbers — port\|retail\|diff"). Machine backing: re-drove the port over `house-pause` HOUSE_FREEROAM+120:240 vs the retail v3 cache — resting-menu **draw program ALIGNED, 10/10 draws content-hash-matched, 0 divergent** ([4] 6q / [5] 1q / [6] 3q / [7] quota 6q / [8] gold 4q / [9] level 1q, every tex/colorop/tri/geometry-hash = retail); **history-replay pixels BIT-EXACT (meanabs=0, gt8=0%) across every resting pair** (offsets 100/126/152/177); port self-verify still **240/240 BIT-EXACT**. `f3ef342`. See `plans/pause-menu.md` M2c. **Scope = resting menu** (fully static at rest ⇒ NO phase residue — bit-exact, not just "looks 1:1"). The open-animation slide is the same load-seam phase pillar as M3 (DEFERRED). **PORT-DEBT(pause-xp-anim):** the XP-display animator (shared with the merchant HUD's stubbed `set_xp`) stays unported — only matters mid-rank-up. ⇒ **the WHOLE resting pause menu (backdrop + option list + header + calendar + numbers) is now confirmed 1:1.** | 2026-06-13 |
| **Pause SAVE submenu — the save-slot card list (M4, mode 9)** | ESC → 3×down → Z opens the type-3 Save submenu: the full save-file selection list (`FUN_0049b556`, ported as `save_picker.{c,h}`): the ADDSIGNED card boxes + per-card file# / rotating clock-hand portrait / day# / gold / "Merchant Level N" plaque + level digits / SCORE·LOOP·TIME, with "NO-DATA" for empty slots + the scroll arrows; driven by the type-3 commit + `FUN_004812e4` wrapper + the `sub_anim>0` render dispatch. | **user confirmed** "looks good to me", 2026-06-14 (feed "M4 pause Save submenu — port\|retail\|diff @ rest"). Machine backing: re-drove the port over `house-pause-save` PAUSE_READY+250 (at rest) vs the retail v3 cache — `orv3_shot` per-frame render **PIXEL-1:1, meanabs 0.13 / gt8 0.00% / max diff 2** (occupied slot-0 card: portrait + Merchant Level 1 + SCORE 0 + LOOP 0 + TIME 0:03:50 + the NO-DATA cards + headers all match). +4 host tests (3253). `7affa5f`. See `plans/pause-menu.md` M4. **Scope = the resting RENDER.** Known OFF-SCREEN draw-program divergence (zero pixel impact, accepted): retail draws 3 carousel pages (center + L/R wings, gate `DAT_09643520>=10`), the port 1 (3.00× box geometry) — the wing gate is the title continue picker's open-ramp left at 10 post-Continue; `PORT-DEBT(save-picker-wings)`, quirk §124, closes with the title-picker render port. Verify caveat: the per-frame `--verify-hashes` self-verify is DIVERGENT (can't rebuild the RT backdrop, M3 limit) — `orv3_shot` composites it, so its pixel-diff is the valid check. **PORT-DEBT:** the picker NAV (`FUN_0047f5bc`, M4b) + the disk-write COMMIT (`FUN_004905a8`, M4c) — the trace opens the picker but never navigates/presses A. | 2026-06-14 |
| **Pause SAVE submenu — the picker NAV + the shared-globals fix (M4b, mode 9)** | the Save submenu slot-picker driven through a full nav: U/D ±1 cursor (the occupied slot-0 card scrolls up under the NO-DATA cards via the c894 row-slide), L/R ±3 paging (c898 column-slide), and B-close back to the option list (`FUN_0047f5bc` → `pause_save_submenu_update`). PLUS the selected-card breathe brightness + the off-screen wing pages. | **user confirmed** "can confirm the save picker is 1:1", 2026-06-14 (feed "M4b … port filmstrip" + "NAV is 1:1" + "breathe+wings FIXED"). Machine backing: new `SAVE_PICKER_READY` anchor (port + frida agent) rebases the nav past the per-side-variable pause-OPEN ramp; re-drove the port over `house-pause-save-nav` SAVE_PICKER_READY+0:240 vs the retail v3 cache (join 239/239) — **PIXEL-1:1, worst gt8 0.000% across all 240 nav frames** (the breathing-aligned frames + the post-close option list bit-identical) AND **draw-program 1:1, 0 draw-divergent** (was 169). The breathe (`_DAT_09643574`) + wings (`DAT_09643520`) were the engine's ONE shared render (`FUN_0049b556`) modeled as two divergent port copies — the title Continue picker (retail runs it ~100+ frames) never carried its history into the pause; fixed by the title render driving the SHARED `g_save_picker_frame` + mirroring `g_save_picker_hpage_anim`. +13 host tests (3265). `b46858a`+`461d873`. See `plans/pause-menu.md` M4b; quirks §124/§125. ⇒ **the save picker (render M4 + nav M4b + shared globals) is now fully 1:1.** **PORT-DEBT:** the A-confirm + disk-write COMMIT (`FUN_004905a8`, M4c) — the nav trace never presses A. | 2026-06-14 |
| **Pause SAVE submenu — the A-confirm + COMMIT (M4c, mode 9)** | A on the cursor's slot commits a save: an EMPTY slot commits at once, an OCCUPIED slot pops the "Overwriting file. Are you sure?" choice box → Yes runs the 60-frame commit animation (the save-progress bar on the selected card + the save-pulse + the working-bank→slot disk write `FUN_004905a8`), back to the resting picker (`FUN_0047f5bc` A-branch + `save_io_commit_slot` + `FUN_00434def` dialog + the `FUN_004812e4` progress bar). | **user confirmed** "the push looks good", 2026-06-14 (feed "M4c pause SAVE commit — port\|retail\|diff": [1] Overwriting dialog [2] save-progress bar [3] post-commit rest). Machine backing: new `house-pause-save-commit` trace (ESC→3×down→Z→{SAVE_PICKER_READY}→A on slot 0→Yes→commit) vs the retail v3 cache — `orv3_shot` per-frame at the **port#N vs retail#N+1 (1-frame async-pause seam) alignment: ~0.12% / meanabs ≤0.10 across the whole commit window** (dialog 0.07%, commit ramp + progress bar 0.12%, post-commit card 0.12% — within M4's accepted breathe/seam envelope); the only elevated frame is the single dialog-close transition (1.86%, the box text 1 frame off at the seam). +6 host tests (3270). `f819e53`. See `plans/pause-menu.md` M4c. **Scope = the OCCUPIED-slot overwrite→commit path** (the verified trace). PORT-DEBT(save-commit-dungeon): the dungeon "Saving here…" warning + the town-state swap (inert in the house); PORT-DEBT(save-card-type-modes): the mode-6/0xb card-snapshot type (pixel-invisible). The EMPTY-slot direct-commit branch is host-tested + uses the same (verified) commit machinery; its visual is unverified (trace `house-pause-save-commit-empty` queued, not yet driven). ⇒ **the save picker render (M4) + nav (M4b) + commit (M4c) is now 1:1.** **NB (post the 2026-06-14 `playtime-ticker` fix):** the card's `TIME` now ticks (the working playtime increments each frame) → in this PINNED trace it shows a load-seam offset (port 0:04:03 vs retail 0:05:14, retail's slower pause-load accumulates ~71s more) — the db054-class load-dependent phase pillar, NOT a commit-logic gap; the commit mechanics (dialog/progress-bar/write) remain 1:1. Normalize via a `{phasepin}`-playtime extension for a frame-exact TIME. | 2026-06-14 |
| **Pause menu UNPAUSE — resume the scene in place (mode 9, the unpause-restore)** | ESC out of the pause menu resumes the underlying scene EXACTLY where it was — Recette stays at the position she was paused at (not re-seated at the scene spawn). The engine never reloads on unpause: it freezes the scene through the open ramp (per-mode dispatch skipped while `g_sim_counter_998 != 0`) and resumes it in place on the close ramp; the port dropped its wrong worker re-spawn and runs the engine teardown (`FUN_00453384` L50254-50283: stage-pulse clear / cursor hide / equip re-aggregate / `d3d_pool_release_type(0xc)` asset free / hand-cursor restore gated on the pause-open visibility snapshot). | **user confirmed** "can confirm the pause menu behaves correctly now", 2026-06-15 (feed "pause-unpause-restore: resumed scene (port vs retail)"). RE correction: `DAT_06a499ac/b0/b4` are the shared HAND-CURSOR snapshot, NOT a player pose — the reseat was the port re-spawning the load worker (re-running the INGAME case-1 load `scene1_preload_house`→`scene1_postload_pose_player`), not a missing restore. Machine backing: new `house-pause-unpause` scenario (walk RIGHT off spawn → ESC pause → ESC unpause) vs the retail v3 cache (join 139/140) — the state panel's **player px/py is bit-identical port==retail at the walked-to (+3.10,+6.04) across all 112 aligned pairs** (open→unpause→resume; 0 mismatches), never jumping to spawn (−0.30,+9.35); resumed-scene pixels match retail (gt8 0.52% @ mean\|abs\|=0.00/ch = the pre-existing benign HOUSE 98-vs-125 batching). +3 host tests (3273). `818af18`. See `plans/pause-menu.md` PORT-DEBT registry. **Scope = the HOUSE ESC unpause** (action 0). **PORT-DEBT(pause-shop-restore):** the shop-grid unpause rebuild (`FUN_00468338`, inert outside a shop-display pause) + the guild-pause re-init (saved_mode==6) + pause actions 1/2 stay deferred. | 2026-06-15 |
| **Pause ENCYCLOPEDIA submenu (type 6, mode 9) — grid + nav + item-detail overlay** | ESC → Encyclopedia opens the item catalog (図鑑): a 33-category horizontal carousel of 3-column item grids (icon+name, the packed-discovered slot table sorted by rank+id), the completion-% panel, the bottom description, scroll + category arrows (`FUN_0049f012` setup / `FUN_0049efb8` cursor / `FUN_0049f365` nav / `FUN_0049f8b8` render → `src/encyclopedia.{c,h}`); the carousel nav (R/L-shoulder page + Right/Left/Up/Down within the full grids); and the A-press item-detail overlay (`FUN_0046a336`): an item stats card (Title / Type / Effect [equipment ATK/DEF/MAG/MDEF · food Recovers HP/SP · medicine Gives Max HP] / Base Price / Highest+Lowest Sale Price) over a data_win panel. | **user confirmed** "can confirm the encyclopedia feels right and looks 1:1 visually", 2026-06-15. Machine backing: re-drove the port vs the retail v3 cache on the new `ENCYCLOPEDIA_READY` anchor (joins 239/239, 559/559) — **grid + nav gt8 0.0000% BIT-EXACT** on BOTH the usual `house-pause-encyclopedia(-nav)` save AND the maxed `house-pause-encyclopedia-max` save (every item's icon+name across all 33 categories; the mid-category-slide transients = the accepted 1-frame async-pause seam × full grids), and the **item-detail overlay gt8 0.0000% BIT-EXACT** across open→shown→close on `house-pause-encyclopedia-detail` (usual: Worn Sword) AND `house-pause-encyclopedia-max-detail` (maxed: 6 categories incl. the populated Highest/Lowest Sale Price lines). Also FIXED a latent pause bug the maxed full-grid nav caught — the shared hand-cursor slide (`FUN_004356cd`) was never ticked during mode 9 (the Save submenu hides its cursor so it never surfaced); ported the tick to `pause_menu_update`'s tail, no regression (house-pause M2/M3 stays bit-exact). +9 host tests (3281). `b7318e7`→`4fcc1d3`. See `findings/encyclopedia-RE.md`. **PORT-DEBT(encyclopedia-detail-combine):** the detail's combine/recipe icon grid is gated on the recruited-adventurer model (`DAT_0741bed8`, the `pause_status_count` stub 0) — a no-op (doesn't manifest on these saves; matches retail). The food/medicine Effect branches are transcribed 1:1 but exercised only once a consumable is examined (the verified items were equipment). | 2026-06-15 |
| **Merchant's Guild main MENU (resting state)** | the post-cutscene guild main menu at rest (FUN_0049404b): parchment panel + Buy/Sell/Talk/Leave options + gold/clock/Day HUD + the "Guild Master" speech bubble ("Time to stock up a bit, eh?") + the hand cursor + the "New" sparkle badge on Talk. | **user confirmed** "looks good" on the served session at label 02191 (`?session=merchants-guild-20260608-151902`), 2026-06-11. Machine backing: diff frame ~99.9% black — panel/options/HUD/bubble+text/nametag/guildmaster all pixel-identical; only the cursor bob (~3px) + New sparkle phase residuals (DEFERRED below). `06e9fdf`+`6ea5a3a`. See `findings/merchant-guild-RE.md` "Main menu UI". **Scope = resting menu only**; the interactive nav/buy/sell/talk flow is unported (`PORT-DEBT(guild-menu-nav)`). | 2026-06-11 |
| **Merchant's Guild BUY list (step 2: Buy opens the item window)** | Z on Buy → the main panel slides out + the item window slides IN with the guild buy stock populated: the Swords tab (Worn Sword, Longsword), item icons, the "%s - %d Left" daily-cap rows (3 Left / 1 Left), the description (text + "Purchase Price- 140" + "Number possessed- 0"), and the Item-Details hint. | **user confirmed** "the swords menu indeed matches", 2026-06-11, on session `merchants-guild-ui-flow-20260611-052747`. Machine backing: the port's frozen fresh-open frame is item-identical to retail's pre-overlay frame (names/order/icons/caps/desc/price/possessed). Port of `FUN_0049196f` (buy scan) + `FUN_00468338` mode-7 tabs + the buy-row/price-label render; `4f80ed6`. See `findings/merchant-guild-RE.md` "BUY FLOW step 2". **Scope = the fresh OPEN only** (the port freezes there); item-list nav + the qty/"yes-no" confirm overlay + purchase are steps 3-4 (next). PORT-DEBT: price-trend factor + the Out-Of-Stock/Not-For-Sale post-buy status texts. | 2026-06-11 |
| **Merchant's Guild BUY qty prompt (steps 3-4: item-select → "Yes/No" overlay → purchase)** | A on a buy-list item opens the "Buying N <item>. Are you sure? Yes/No" qty overlay (`FUN_00491bc0`/`de0`): U/D qty, L/R Yes/No, A buys / B cancels; the green-outlined qty number, the "Stock Price␣␣␣␣␣Npix" total, the up/down arrows, and the **gold rolling down on purchase**. | **user confirmed** "the panel looks correct other than the slight phase desync that was already there", 2026-06-11, session `merchants-guild-ui-flow-20260611-052747`. Then 3 user-flagged polish gaps fixed + machine-verified vs retail: green qty outline (port 205 green px @label2012 vs retail 221), full-width-space price spacing ("140pix" x230 vs x228, was overlapping "Price"), gold rolling-counter (`FUN_00406584`: 1000→860→580 across buy1/buy2 **matching retail at the same labels** = roll RNG in sync). Cutscene bit-identical after the global font change (diff 0px>8 @labels 900/1100). `45f5bca`+`922b5be`. See `merchant-guild-RE.md` "BUY FLOW" 3-4. **PORT-DEBT:** Sell (mode 3), first-buy tutorial/limit gates, price-trend factor; residual 14 nav/select SE (qty auto-repeat cadence + retail-only-region). | 2026-06-11 |
| **Merchant's Guild Talk submenu (`FUN_004922c0` mode 2)** | A on Talk opens the submenu: the "Talk" header + 6 topics ("What is the guild?" … "About fusion") + per-topic "New" sparkle badges, **no-wrap U/D nav** with scroll revealing the 7th "Never mind" close row, A→topic dialogue (iv1_10/11/12/14/24/25) + per-topic seen-flag, B/Never-mind close. | **user confirmed** "can confirm the talk menu looks correct", 2026-06-11, session `guild-skip-dialogue-talk-leave-20260611-204101` (served :8778, `caprange [250,1250]`). Machine backing: studio diff **bit-identical** at the open (label 1015, cursor "What is the guild?") AND the scrolled state (1140, cursor "Never mind", correctly badge-less, no-wrap held) — only residual = cursor-bob + New-sparkle phase (the load-seam phase pillar, same class as the main menu; DEFERRED). Port of the mode-2 state machine + main-list slide/cull + submenu render; `88b666a`. See `merchant-guild-RE.md` mode map "2 = Talk submenu". The 17 audio misses are all post-window (retail 15984-18750 = the buy/leave/bread the port stalls at — Leave is the next gap). **PORT-DEBT(talk-confirm-flash):** the c20 selected-row brightness pulse. | 2026-06-11 |
| **Merchant's Guild first-visit cutscene (iv1_3)** | the full mode-6 first-visit dialogue, frame-by-frame: guild bg + guildmaster/Tear/Recette standees + text box + text reveal + the standee slide tween. | **user confirmed** the anchor-matched port\|retail montage ("that looks correct", 2026-06-10). Machine backing (census): 75/75 cutscene anchors frame-exact · 8 `dialogue_tick` fields × 774 frames ZERO mismatches · rngcalls 0 desyncs / raw rng 714/714 bit-exact · 7/9 sampled frames pixel-identical (MSE 0.00). `flow_diff --verdict --align-anchor TEXT_ANIM_START` = ✅ PHASE-CLEAN. Residual = load-suppression seam only (phase, accept). See `findings/merchant-guild-RE.md` "CENSUS DONE". | 2026-06-10 |
| **Merchant's Guild ESC-skip hand-cursor restore** (skip a dialogue with ESC → the shared hand cursor returns to its pre-box row instead of vanishing; `FUN_0046c2cb` arm-snapshot + `FUN_0046c320` close-restore → `skip_event.c`, `FUN_00435644` capture → `title_save_dialog.c`) | **user-confirmed** "can confirm the bug is fixed", 2026-06-11. The choice box's close snaps the shared cursor offscreen to (0,-64); Phase C had dropped the engine's resume-state snapshot/restore as PORT-DEBT (harmless for the prologue, wrong for the Talk submenu where the cursor sits on a row), so after a skip it stayed parked offscreen until a nav re-armed a slide — retail restores it. Ported the arm-snapshot `(visible, slide-target pos)` + the close-restore (`e2c ? FUN_00435693 : FUN_00435612`); host-tested ×3 (`test_skip_event`). Machine backing: the SAME latent gap on the first-visit-cutscene skip is closed too — direct frame compare on `guild-skip-dialogue-talk-leave-20260611-204101` **label 800** (menu resting post-skip, pre-nav) = **port == retail, cursor on Buy** (old code was offscreen there). `a7209a2`. See `esc-skip-event.md` "Guild (mode 6)" gap 3. | 2026-06-11 |
| **Merchant's Guild try-leave "no purchase" reminder (iv1_9)** | `A` on **Leave** when the per-location first-leave flag (`save[0x2bc5d]`/`DAT_0450f3f5`) is clear — i.e. trying to leave BEFORE buying — fires the Tear *"You have not yet purchased anything! …Please properly stock up…"* dialogue overlay (`scene1_intro_dialogue_start_single(1,9)` = `FUN_0044ba2c(1,9,0)`, `LAB_00492ad7`) + SE 0x13d, with the **main menu (Buy/Sell/Talk/Leave + the "Well, come back any time!" bubble + hand cursor) rendered frozen behind it**. (A *separate* dialogue from the proper iv1_16 bread cutscene on the actual leave-AFTER-buying.) | **user confirmed** "looks correct to me", 2026-06-12, session `guild-skip-dialogue-talk-leave-20260611-204101` (served :8778 + feed compare "iv1_9 menu backdrop fix"). Machine backing: focused `flow_diff --align-anchor TEXT_ANIM_START --frame-from 17150 --frame-to 17430` (281 frames) = ✅ **PHASE-CLEAN** — fade + dialogue_tick (box_open/reveal/line_row/st5) + rngcalls + raw rng all **bit-exact** vs retail (retail arms it @frame17181; order first-visit@14695 → Talk-topic@15858 → reminder@17181). **Menu-backdrop fix (`aa773d0`) — the VISUAL compare caught what flow_diff missed** (render-side, not dialogue_tick): retail keeps the menu behind the reminder, the port had hidden it (menu-UI draw gated on `!dialogue_busy()`); now drawn when `!busy() || (mode==1 && entry_tick>0xe)` (first-visit cutscene + mode-2 Talk topics unchanged). Port label 3010 == retail. Studio pixel diff seam-unusable (8 load-seams, kept-count port≈3860/retail=3740) — verified via the anchor-aligned flow_diff + content-matched frame. `b5ba796`+`aa773d0`. See `merchant-guild-RE.md` "Leave dispatch RE". **Scope = the iv1_9 reminder only**; the proper iv1_16 bread cutscene + leave→world-map + return-to-Recettear Tear cutscene = `PORT-DEBT(guild-leave-transition)`. | 2026-06-12 |
| **Recette position + walk phase** (free-roam, **MOVING only**) | exact frame-for-frame on the walk benches; she reads pure-black in the port-vs-retail diff. **Scope = while moving** — her *idle* phase is separately flagged off (see NOT-1:1 + Deferred). | user, on the `house-walk-down-dense` feed comparison | 2026-06-01 |
| **Recette RENDERED walk cell** (free-roam, the drawn sprite) | **1:1, human-confirmed.** Earlier flagged as a "different walk cell" on ~18% of frames; that was a **capture-tooling off-by-one** — the Frida agent read the watched state at `input_poll`, one sim-tick before the render, mislabeling every screenshot by 1 (turbo's sim/Present decoupling made it run-to-run variable). Fixed by reading state at capture time (Present onEnter → `frames_meta.jsonl`, 715b74c); the same TURBO capture is then bit-1:1 (arms-diff ≤1.4, many 0.00) incl. wrap frames. | user "that capture is fully 1:1" on the capture-time-aligned montage + no-turbo bit-exact + deterministic port cell-log | 2026-06-04 |
| Player walk + collision (HOUSE) | bit-exact px/pz vs retail (mesh resolver) | engine-quirks §60–70; wall_collide_diff | 2026-05-31 |
| **Character ground shadow** (player+Tear, Csh.1) | **user-confirmed 1:1** | user (explicit), + feed `20260601T122354_6f81` cap_06 feet zoom | 2026-06-01 |
| **Tear (companion) — full free-roam sprite** (body anim, additive wing-glow, **and** type-0x1f wing-sparkle particles) | **user-confirmed 1:1 ("yes that is 1:1")** given phase+RNG pinning. `phase_probe house-idle-npc-drift` (auto-adds `{phasepin}`+`{rngseed}`) = **PHASE-CLEAN** (companion `cframe/ccnt/coct/canim` + raw LCG bit-exact +2…+258); pinned pixel-diff full frame **0.04%/mean0.00** (Tear crop cap_00 0.55%, cap_05 0.05%). The long-"persistent" residual was **RNG-phase noise** (the scenario trace had `{phasepin}` but no `{rngseed}` → sparkle particles at a different LCG origin), NOT position/anim/render. Spring-follow position is also 1:1. | user (explicit, feed montage) + `phase_probe` verdict + full-frame pinned diff | 2026-06-05 |
| Foot-dust EMIT cadence | every 16 frames (median gap 16) | Frida ground-truth probe | 2026-06-01 |
| **Furniture shadows** | **user-confirmed 1:1** — the real baked 3D meshes (real mesh loader), NOT a placeholder; table-base zoom-diff all-black | user (explicit) + `house-walk-tables` cap_10 zoom-diff, feed 2026-06-01 | 2026-06-01 |
| **Opening dialogue fade-from-black** (chr:5 kuro.tga, alpha 255→0 over fadeframe:240) | **user-confirmed 1:1** — render + the per-frame fade curve are bit-identical **when phase-aligned** (anchored to the fade's own `EXTRA_SPRITE_FADEOUT` edge; the *absolute* HOUSE_FREEROAM start-offset drifts — synthetic-load phase, engine-quirks §85). | user (explicit, feed) + `intro-fade` 0.07 mean\|abs\|/ch (residual = FPS overlay) | 2026-06-02 |
| **Opening dialogue extra/effect sprites** (sigh tameiki / zzz pop-ups) | **user-confirmed bit-identical** — render AND **within-script phase** are 1:1 with **no artificial sync**: captured at a *fixed* offset from `TEXT_ANIM_END` (no per-effect anchor) the diff is 0.06 mean\|abs\|/ch (residual = FPS overlay). Only the absolute HOUSE_FREEROAM offset drifts (§85, common to all prologue timing). | user ("bit identical") + `intro-sigh` / TEXT_ANIM_END-relative re-capture | 2026-06-02 |
| **Opening dialogue per-line** (bedroom bg, standees incl. Tear's −390→−100 slide-in, box, nameplate, char-reveal/book-icon, text) | user-confirmed 1:1 vs the per-line goldens (slide-in + char-based reveal landed 2026-06-02). Remaining real deltas tracked separately: box-edge halo + FPS overlay. | user (feed, cap_00/cap_01 + slide) + `intro-dialogue-lines` (line 1 diff = box-edge only) | 2026-06-02 |
| **Bottom-left "Merchant Level" HUD** (badge disc + level number, gold "Merchant Level" label, XP bar; `FUN_00409925` body → `scene1_merchant_hud.c`) | **user-confirmed 1:1** ("the hud is definitely 1:1"). `pixel_diff` of the bottom-left HUD region at native 640 = 0.07% (13/19008 px), white-diff black. Gating also confirmed: shown in free-roam + iv1_2 dialogue, hidden during iv1_1 (engine-quirks §87). Dormant/deferred: the "LEVEL UP!" pop (`FUN_00407ab4`, needs the level-up event subsystem to drive `DAT_0438b920`). | user (explicit) + `house-walk-table` vs `runs/changecam-trace2` retail, `pixel_diff` crop 0,414,288,480 | 2026-06-03 |
| **Skip-prompt hand cursor** (ESC "skip this event?" choice box's bobbing hand; shared `FUN_00435747` via `title_save_dialog_cursor_*`) | **user-confirmed 1:1** ("the cursor is definitely 1:1"). Unified onto the one shared cursor (f9708c7); bob counter `b154` phase-pinned (`{phasepin}` zeroes it at `TEXT_ANIM_END`, cb4002c) so the `\|sin(b154·.1)\|·8` wobble lands bit-identically (hand-left 332==332 px). Unpinned the bob phase is §85 load-dependent (engine-quirks §100). | user (explicit) on the `intro-skip-prompt --target both` feed push | 2026-06-05 |
| **LOAD GAME / continue slot picker** (the save-card grid: parchment cards, clock-dial panel, rotated day hand, day/gold digits, money banner, level badge, SCORE/LOOP/TIME, NO-DATA, scroll arrows + "LOAD GAME" banner; `FUN_0049b556` → `scene_title.c`) | **user-confirmed effectively 1:1 (1-px notice).** Was a placeholder text list; now the faithful render. `title-load-picker --target both` (fa7c82 save): slide-in frame **0/786432** diff px, settled frames **1 px** (max Δ20 — sub-pixel on the clock-dial hand). Card text scale 0.8 vs slot#/NO-DATA 1.0 (user spotted the size gap pre-fix); brightness/scale constants objdump-recovered (engine-quirks §101). | user ("looks good… effectively 1:1 with the 1px notice") on the `title-load-picker` retail\|port\|white-diff feed push | 2026-06-05 |
| **Title menu items** (the `fuki.tga` menu options: selected-item pulse + unselected greys; `FUN_0049c644` → `scene_title.c`) | **user-confirmed 1:1.** Selected item pulses bit-identically (`0x828282`→`0xb8b8b8` f30→f60); unselected greys were too bright (`0x95`=149) and are now `0x5f`=95 = retail. After fix the port is **pixel-identical** to retail (0 px diff >10; was 13098 px in the menu region). Root-caused via the render_quad flow-trace (diffuse `0xff5f5f5f`) + the engine denormal `1.33123e-43`=bits 95 (engine-quirks §97). | user ("confirmed 1:1") on the `boot-idle` before/after/retail/diff feed push | 2026-06-05 |
| **Companion (Tear) idle FACING on a CONTINUE-load** (loaded-shop free-roam, fairy stationary from entry) | **user-confirmed 1:1** ("basically fully 1:1 other than the known dots"). Retail faces DOWN (octant 4 = the entry seed); the port forced octant 2 via `FUN_0048a833`'s intro-only `(comp.x<=player.x)?6:2` branch A. The free-roam law `FUN_0048a4d1` writes facing only on a *moving* frame, so idle HOLDS the seed (engine-quirks §109). `flow_diff --verdict --align-field db054`: `coct` retail 4 / port 2 → **ALIGNED** after the fix; every other actor field bit-1:1, whole frame PHASE-CLEAN. NB the player facing was already 1:1 (oct 6 both) — the flow-trace overturned the FRONT hypothesis that Recette faced wrong. | user (explicit, feed) + `flow_diff --verdict` on `house-loaded-display-pinned --target both --call-trace` | 2026-06-06 |
| **Background-window NPCs** (the 6 townsfolk drifting past the shop's back window; `scene1_bg_npc`, `FUN_0046f2a3`/`621`/`648`/`737`) | **user-confirmed 1:1** ("that 1:1, nice!"). The 6 NPCs ride the **shared LCG**, so their drift state is load-RNG-history-dependent. (1) **In dialogue captures:** `{phasepin}` re-runs the warmup from a canonical seed (19937) on BOTH sides — each runs its own spawn/drift code, so a match *proves the logic 1:1*; every iv1_2 line's window band 2478px@mean0.76 → **424px@mean0.00** (≤1 LSB). (2) **In live free-roam:** they now **STAY** 1:1 — `house-idle-npc-drift` (one pin then free-run) holds **≤69px@mean0.00 across +2…+260** after the §95 dev-overlay RNG fix (was desyncing at +100). | user (explicit, feed before/after) + `intro-dialogue-lines` + `house-idle-npc-drift --target both` | 2026-06-05 |
| **Faced display-cell highlight glow** (C3a — the orange `item_win` decal pulsing over the display stand the player faces in free-roam; `FUN_0045aa36` **Block G** → `chr_shadow_build_display_glow` + the Block-G draw in `scene1_chr_shadow_render`, commit e25587c) | **user-confirmed 1:1** ("can confirm the glow is 1:1, good work"). item-display gap #1. The faced cell (4,4) shows the pulsing orange highlight (alpha 127↔191, `sin(g_sim_frame·0.05)·32+159`); on the `item-display-2` port recapture the studio diff `diff/frames/frame_00107.png` glow-region (y380:475,x455:560) is **max 6/ch, mean 0.00, 0 px >8/ch** (bit-clean). Bit-exact constants: scale **0.0036799998** (`0x3b712c27`), UV texel **centres** 224.5/287.5/480.5/543.5 over 1024, alpha consts all float (the RE doc's "0.003685" / "225·288" were lossy roundings). engine: gate `cc08==1 && bf68==0 && cbfc!=-1 && cc00!=-1`, SRCALPHA/INVSRCALPHA + COLOROP MODULATE. | user (explicit) on the `item-display-2` studio session diff | 2026-06-09 |
| **World-map (town map, mode 8) cursor NAVIGATION** (the destination-pointer 3×5-grid walk; `FUN_0049dfc1` → `scene_worldmap_cursor_nav`) | **user-confirmed 1:1** ("nav looks good, everything's perfectly aligned"). The full ~20-step nav sequence tracks retail's selected-marker **end-to-end** on `town-map-load-rerecord`. The port was stuck on dest 0 ("Recettear") **not** because of the nav logic (T4-verified 1:1) but because the port never emitted the **`PAUSE_OPEN`** anchor at the world-map load — retail's init raises the shared cursor `DAT_0438b150=1` (§115), which the recorder keys PAUSE_OPEN on, so the trace's `{wait PAUSE_OPEN}`-gated nav-input segment never replayed (`held=0`/`sel=0` every frame). Fix: `main.c` `pause_active` += `scene_state==SCENE_STATE_WORLDMAP` (commit f7275e4). Verified on a re-windowed + `{capstride:10}` both-capture (caprange at the 2nd LOADING_END, port==retail==63 frames, aligned by frame#): white-diff **black** across bg + markers + selected-marker + cursor at wm-frames 70/240/410/580; only the #5 travel-time tooltip box differs (PORT-DEBT). | user (explicit) on the aligned `town-map-load-rerecord` studio `diff.mp4` + feed montages | 2026-06-08 |
| **bg-window NPCs THROUGH the tutorial dialogues** (the event-arm routing: `FUN_004536cb` dispatches `DAT_0438b1c8`-busy frames to `FUN_004427d3`→`FUN_0048407f`, which pumps `FUN_0046f621` + db054++ — commit 843b6f1) | **user-confirmed** ("can confirm that the NPC desync is gone"). Pre-fix the port ran the DEFAULT arm during dialogues, consuming dev-overlay LCG (+1/f) + 目玉 sparkle (+6/8f) retail doesn't → +3245 calls over the two dialogue windows (net +1375), desyncing every shared-LCG consumer incl. the bg walkers. Post-fix both sides consume +6 every 4th frame (wing) and nothing else during dialogue (port 1.52/f ≡ retail 1.51/f); `flow_diff --verdict`: rngcalls net +31 (seam-boundary residual), px/py/dust/db054/cbfc ALIGNED/bit-exact. Same chip fixed the db054 menu-close off-by-one (+1 per pause). NB the user's first look was clouded by a STALE-pairing diff (ordinal-paired `build_diff` ghosting the movers — fixed to label pairing the same day); the same-label frames were already 1:1. | user (explicit, studio session item-display-2 post-recapture) + flow_diff --verdict | 2026-06-10 |
| **Placement-prompt speech bubble** (gap B — "What will you place?" / "Exchange with what?" / "Place Vending Machine"; the FUN_0046b00a screen-space block → `scene1_display_menu.c` render + `display_menu_set_window_flag`, commit 36a8ab2) | **user-confirmed 1:1** ("can confirm bubble … 1:1"). All 3 prompts are BAKED localized item_win.tga sprites (NOT strings — absent from exe+data in every encoding; the RE doc's "world-projected message-table string" guess was wrong on every count). dst(menu_x−128,48,191,63) rides the panel slide → the slide-in the session notes pinned at 589/592. Flag set by the cc04 arm (occupied→2, empty→1), Vender-category highlight overrides live. Machine-verified 0-1px at label 439 + slide 588-594 pre-confirmation. | user (explicit, item-display-2 studio recapture) + region pixel-diff | 2026-06-10 |
| **Hands-up carry pose** (db048==0xc placement-confirm carry; r==3 arm + FUN_0048cdcc 0xc branch → `scene1_player_ctrl.c`, commit 4bc8a0b) | **user-confirmed 1:1** ("can confirm … pose are 1:1"). POSE-ONLY (retail draws no overhead item sprite — the old "held item red-vs-gold" was sparkle residue). Anim 4 latch-gated at r==3, 26-frame free-roam hold w/ the 87617/87524 interaction+impulse gates, release re-selects idle. Carry-window frames ~2,290→2-65 px>8; residue = sparkle-particle phase. Label-441 whole-frame diff proven PRE-existing (menu-close camera pan-out, FRONT #6). | user (explicit, item-display-2 studio recapture) + stash-rebuild attribution | 2026-06-10 |
| **Dialogue typewriter reveal fade** (the per-CHAR trailing alpha ramp; `FUN_0047d464` → `font_draw_text_fade` + `dialogue_draw_row`, commit c533c93) | **user-confirmed 1:1** ("can confirm text gradient is 1:1"). Law: char i draws at `alpha·clamp((budget−i)·0.2, ≤1.0)` — budget INIT 0x47d4d4, per-glyph reload `fildl` 0x47d528, per-char `decl` 0x47d60e; trailing ~5 chars ramp 0.2…1.0 riding the reveal head (the ghost trailing char). The 06-09 per-row port (a278101) was a loop-counter misread (gotcha #18); the ALPHA-pipeline suspicion was moot (row fade visibly dimmed ⇒ diffuse alpha flows). Machine: text-strip gt8=**0** on every sampled reveal frame (labels 773-790), session over-threshold 1212→916. | user (explicit) + text-strip gt8=0 on the item-display-2 recapture | 2026-06-10 |
| **2nd tutorial dialogue (iv1_6) cross-seam alignment** (the iv1_5→iv1_6 re-arm timing; `D_TUT_DONE` 1-frame settle latch → `scene1_intro_dialogue.c`, commit c8a40df) | **user-confirmed 1:1** ("can confirm the 2nd dialogue is 1:1 aligned now"). The port armed iv1_6's `LOADING_START` the SAME frame iv1_5's script completed; retail defers the re-arm 1 frame (its gate `DAT_0438b1c8` clears 1→0 in `FUN_004536cb`'s tail AFTER that frame's `FUN_0044bd0d` dispatch ran — call-trace: iv1_5 `FUN_0046c320`-done @f15933 → iv1_6 `FUN_00452d07` load-spawn @f15934). The same-frame arm injected a constant d=−1 on every iv1_6 anchor (last `CONV_POSE_BLINK`→`CONV_POSE_END` 8f port/9f retail), d=−2 after iv1_6's tail. Fix latches a settle frame (`_busy()`/`_posing()` stay true → dispatch skips → arms next tick). Recapture #7 (`--only port`): iv1_5-tail 8f→9f, every iv1_6 anchor bit-aligned to retail (+733/+734/+1166 from iv1_5 `CONV_POSE_START`), `problems: []`, over-threshold 861→529, rngcalls desync +26→+12. Closed the standee@~1448 + NPC@~1844 seam artifacts (RE #8). | user (explicit, item-display-2 studio session) | 2026-06-10 |
| **Display-menu panel SLIDE (open + close)** (the bottom description panel rides the menu slide x-offset; `display_menu_description_render(…, x0)` → `scene1_display_menu.c`, commit d4899bc) | **user-confirmed 1:1** ("can confirm the panel slide in is 1:1"). Was the session's worst frame (label 181 gt8≈185441) — the panel was drawn FIXED at x=0 while retail slid it with the menu. Ghidra dropped the float arg at the `FUN_00469b3a()` call site; the real `param_1` is `640−(DAT_0734b98c<<7)` (the same offset the main panel rides; gotcha #2 worked example). NOT the `pressed&0x40` Item-Details overlay (that is never exercised by this bench). Recapture: label 181 185441→near-black, close-slide bit-exact (`R[f]=0.00`). The remaining open-slide residual (label 125 ~29k) is the user-accepted "±1 animation-frame, no real phase desync" boundary noise (deferred, FRONT #6). | user (explicit, item-display-2 studio recapture) + studio triage (worst 185441→29182) | 2026-06-10 |
| **Item-display AUDIO track** (menu/footstep SEs `af47e08` + dialogue voice-mute-on-fast-forward `e646702` → `scene1_player_ctrl.c` / `scene1_dialogue_run.c`) | **menu SFX user-confirmed by ear** ("verified, sounds correct"); the full track is then `audio_diff` **VERDICT ALIGNED** (0 missing / 0 extra / 9 sounds matched). The cc04 interaction stubbed every SE play (open `00re_sys04a/b/c` rand%3, confirm clip `05b` rand&1, `0x143` beep, walk footstep `0x166` gated `STATE==1 && COUNTER&0xf==0xa`) — un-stubbed, RNG-neutral (pixel/flow triage unchanged). Then the user-flagged voice-mute: retail mutes dialogue voice lines while holding X to skip (the `se:` opcode plays only when step count `DAT_005c78ec==1`; quirk §120) — gated `IVE_OP_SE` on `(held & IVE_BTN_FF)==0`, which removed the 3 held-X voice grunts the port over-played. | user (explicit, by ear) + `audio_diff --session item-display-2` VERDICT ALIGNED | 2026-06-10 |

## CONFIRMED NOT 1:1 (human-flagged — do NOT hand-wave)

| subsystem | what's wrong | note |
|---|---|---|
| **Dialogue box-edge "halo" = `ive_box_scale` cos-vs-sin bug — FIXED + 1:1 (2026-06-05, user-confirmed)** | The `ive_window.tga` bubble "edge halo" was the box's squash-and-stretch scale computed with the **wrong trig function**: the port's `ive_box_scale` used `cosf`, but retail's `FUN_0046c86f` calls `FUN_00503a44` = **`sinf`** (`FUN_00503994` is cos; confirmed across the corpus). The 2026-06-05-AM "box SCALE / bounce-anim PHASE" call was half-right (it IS box scale, not a texel/decode delta) but the cause was NOT a phase mismatch: a per-frame Frida watch (`runs/dlg-box-watch{,2}`: `DAT_073a3e14`/`e00`/`e04`/`6a38` + `--d3d-trace-verts`) proved **`box_open` AND `line_row` are bit-1:1 port↔retail at every captured line (0/46** over `intro-dialogue-lines`, END+2 capture). The box-width-per-frame decoded from retail's verts matches the **sin** table frame-for-frame and **no** cos value. At the settled box (n=15, angle 3π) `sin(3π)=0` ⇒ sx=sy=1.0 (full size); the old `cos(3π)=-1` gave 0.9875/1.0125 — a ~5px-narrower, X-shifted box (box X centres on sx via `local_20=(local_c+208)-sx·208`), rippling through frame+nameplate+glyph text → **~116k px/line** on the iv1_1 bedroom lines. Fix: `src/scene1_dialogue_run.c ive_box_scale` cosf→sinf (engine-quirks §103). **cap_00 vs retail 116539px → 856px** (residual 856 = benign stale-golden FPS corner). Lines 0–15 pixel-clean; 16–45 keep only the deferred iv1_2 freeroam-anim gaps. | user-confirmed "new diff looks 1:1" 2026-06-05. Evidence: decoded box quad verts vs the discrete `FUN(integer-n)` table (port hit it exactly under cos, retail didn't = wrong-trig-fn signature, not phase) + per-frame `box_open` watch (0/46) + feed `dlg-box-fix-compare`. |
| **Skip-prompt `savewindow.tga` banner + text ~−1 LSB — RESOLVED 2026-06-05 (root-caused, fixed, user-confirmed box 1:1)** | The ~−1 LSB on the gold (`[237,200,52]` vs retail `[238,201,52]`) and the text edges was **NOT** TGA/colour precision — it was a **wrong COLOROP**. `FUN_0043537e` draws the banner+text under `D3DTOP_ADDSIGNED` (=8, ≈ pass-through at the 0x7f7f7f vertex colour); the port used `D3DTOP_MODULATE2X` (=5, `texel·0.996` = 1 LSB darker) — `8` had been mis-labelled "MODULATE2X" (which is enum 5). The banner verts/UVs/diffuse were **bit-identical** port↔retail (decoded from `--d3d-trace-verts`), ruling out scale/phase/TGA. The text halo was a second facet: retail keeps ADDSIGNED through the prompt+Yes/No (resets to MODULATE only at the end) and draws them at 0x7f7f7f; the port reset early and drew full-white under MODULATE. Fix (`src/choice_box.c`): ADDSIGNED for the banner, hold it through the text @0x7f7f7f, reset after → box+text+cursor region **0 px** vs retail (both caps). engine-quirks §104. | user-flagged 2026-06-05; box+text confirmed 1:1 by user 2026-06-05 after fix. Residual full-frame delta = prologue bg §85 phase only. |
| **LOAD-GAME picker: rotated clock day-hand ±1 LSB** | The picker's settled-frame residual (947 px ≤4 LSB, max Δ8, bbox x[138-224] y[171-230]) is **entirely the rotated gold clock needle + its hub** — the clock face, ring, pie segments, parchment cards, and 0.8 stat text are all **0 px**. **This is NOT the "scaling-filter" class.** The day-hand quad's *vertices/UVs/diffuse are bit-identical* port↔retail (decoded from `--d3d-trace-verts`, frame 120: both v0(149.175,152.23)…v3(212.256,247.77), u 0.4541→0.4834, diffuse 0xff7f7f7f), so the port's rotation math is exact. The delta is the **rasterizer bilinear-sampling a ROTATED quad**: every pixel hits a diagonal sub-texel position whose 4-texel blend weight differs by ≤1 LSB between the port's GPU and the retail (`cutestation.soy`) GPU — axis-aligned scaled quads sample grid-aligned and agree (→0 px), only the diagonal one disagrees. Not a port logic fix (POINT would diverge from retail's bilinear hand). **Also found:** retail draws **3** rotated quads on this frame, the port **2** — the port is missing one *off-screen* (x≈−875, alpha 0xc8) hand instance, likely a paged-off slot's needle (off-screen → no pixel effect, but a genuine structural gap). | user-confirmed "it's the clock hand" 2026-06-05. Evidence: `picker-residual-crop` feed push + `render_diff`-decoded strip verts. Distinct mechanism from the box-edge-halo row above (that's axis-aligned, suspected TGA/colour-precision; this is rotated-bilinear). |
| **iv1_2 opening: Recette look-up + blink** | during the iv1_2 (2nd dialogue) opening (post-fade, during char slide-in) retail plays Recette's HOUSE freeroam sprite a look-up-at-Tear pose + a blink (≥3 cycles); the port is static. | user-flagged 2026-06-02. Freeroam-sprite anim during the dialogue (NOT the standee). Reference: `intro-iv2-gap` scenario (zoom (520,578) 104×150, cap_03 open→cap_04 closed). opening-prologue.md §"Remaining real deltas" #4. |
| **iv1_2 opening: Tear angry pose + radial-lines** | same window — retail plays Tear an angry pose with a billboard radial-lines (manga anger-marks) effect; port absent. | user-flagged 2026-06-02. Same `intro-iv2-gap` zoom frames both Recette + Tear. opening-prologue.md #4(b). |
| **Dialogue text fade-to-transparent on dismiss** | when a line is dismissed retail fades the glyph text out as the box closes; the port pops it off. | user-flagged 2026-06-02. `intro-iv2-gap` captures the dismiss window. Box-close alpha on the text in `FUN_0046c9a2`. opening-prologue.md #5. |
| **Recette idle animation phase** | idle pose **slightly off** at `house-movement` **cap_00 (first frame, idle free-roam)** — while her **walking is confirmed 1:1** (cap_01/02). **Narrowed 2026-06-04:** `phase_probe house-idle --window 120,80` shows the player idle anim cycle (`p.aframe/counter/anim/oct`) **machine-verified bit-exact** vs retail once `{phasepin}` normalizes the origin (now covers actor 0) → the idle anim *law* is 1:1; the visible cap_00 diff is the load-dependent phase ORIGIN (harness-normalized, not re-seeded per the shipped game), **plus** a separate idle-only RNG over-consumption (see Deferred) that can shift idle sparkle/effect phase. | user, 2026-06-01. NOT position (pure-black once moving) and NOT an anim-counter logic bug. Chase the visible residual via the idle-RNG gate + a phasepin'd idle pixel-diff; do NOT hand-wave as jitter. ([[project_next_char_controller]]) |
| ~~**Tear (companion) appearance**~~ — **RESOLVED → 1:1, 2026-06-05 (user-confirmed "yes that is 1:1")** | The "persistent" Tear free-roam residual (drift bbox x[522-567] y[370-441] at `house-idle-npc-drift` cap_00, ~2000px@mean0.95, present even +2 post-`{phasepin}`) was **entirely RNG-phase noise**, NOT position, NOT anim-phase, NOT a render bug. The `house-idle-npc-drift` scenario trace carries `{phasepin}` but **no `{rngseed}`**, so the wing-sparkle particles (type-0x1f, RNG-jittered) spawned at a different LCG origin port↔retail and the whole Tear region lit up. **`phase_probe house-idle-npc-drift` (auto-adds `{rngseed}`) verdict: ✅ PHASE-CLEAN** — companion `cframe/ccnt/coct/canim` AND raw LCG (`rng`/`rngcalls`) all **bit-exact** vs retail at every frame +2…+258. Pinned pixel-diff: Tear crop cap_00 14.59%/mean0.77 → **0.55%/mean0.00**; cap_05 (+258, furthest from pin) **0.05%**; **FULL frame 0.04%/mean0.00 = bit-1:1** (diff panel black, feed-verified). So the companion sprite anim, wing-glow, AND wing-sparkle particles are all 1:1 given phase+RNG pinning. Supersedes the earlier "spring-follow-position and/or wing-flap+particle phase divergence" — the spring-follow position is also 1:1 (the body silhouette already overlapped pre-RNG-pin). | resolved 2026-06-05; method = `phase_probe` (one-command phase+RNG verdict). NB §73 "wing-sparkle renderer unported / invisible today" is **stale** — the particles render and match. ([[project_next_char_controller]]) |
| ~~**Recette RENDERED walk cell**~~ — **RESOLVED → 1:1, 2026-06-04** | was flagged as a "different walk cell" on ~18% of frames; root cause was a **capture-tooling off-by-one** (Frida watch read one sim-tick before the render, mislabeling screenshots), NOT an engine divergence. Fixed (715b74c) + user-confirmed 1:1. See the CONFIRMED-1:1 row above + `scene1-recette-walk-cell.md`. | resolved 2026-06-04 |
| **Foot-dust position/phase** | diverges from retail | likely free-roam RNG-stream completeness ([[scene1-rng-stream-parity]]) — but treat as a real structural gap to CLOSE, not "just RNG" |
| ~~**Ambient motes** (`FUN_0046f648`)~~ — **RECLASSIFIED 2026-06-02** | this was never ambient motes: it is the **background-window NPC system** (`scene1_bg_npc`, the townsfolk drifting past the back window). Sim + dark contact-shadow were ported 2026-06-01; the **bright character sprite** (`FUN_0046f737`) landed 2026-06-02 and is **user-verified rendering** (in-game + feed). See [[scene1-bg-npc]]. Residual: exact anim-phase/identity vs retail (deferred), and the genuinely-faint "tiny dots" that may be a *real* separate ambient effect not yet found (do NOT re-conflate). |

## REGRESSIONS caught + fixed this session

| regression | cause | fix | confirmed |
|---|---|---|---|
| Tear's blue wing-glow vanished | b1acf7c sprite full-quad Z-write occluded it (Tear's off-position Z in front of the glow) | revert (957af8c) | user: "the glow is there I promise" |
| Rectangular dust/shadow hole around Recette | same b1acf7c Z rectangle clipped the dust | same revert | user: "yes that fixes it" |

## Still UNVERIFIED (don't assert either way)

- **Opening-prologue dialogue cadence — MACHINE-frame-exact, awaiting user
  eyeball.** A `scenario-test intro-dialogue-lines` port↔retail diff (2026-06-01)
  found **44/45 inter-line TEXT_ANIM_END gaps identical to the frame**; the lone
  difference is the iv1_1→iv1_2 transition (retail +103 frames = the deferred
  inter-script load screen). Strong, but it is an automated frame-diff, not a
  user-eyeballed render — and the per-line TEXT/box PIXELS are still deferred
  (draws not ported), so do NOT yet claim visual 1:1. See
  `docs/findings/opening-prologue.md` §"port↔retail cadence is frame-exact".
- Wing-glow size/intensity exact match (port glow looked smaller at one matched
  frame — never human-confirmed; could be Tear position).
- Object/furniture contact-shadow-BLOB pass (`FUN_00470385` object table,
  `DAT_073a6e84`) — still **stubbed** (no src ref); retail draws ~6 soft multiply
  blobs under objects every frame (draw-order GT). An *augmentation on top of* the
  already-matching baked mesh shadows; visible impact small/localized
  (house-render-gaps §4). ZWRITE=0 → irrelevant to dust occlusion. The sibling
  background-NPC contact-shadow render (`FUN_0046f648`, `scene1_bg_npc`) that
  `FUN_00470385` also calls IS ported; the object-table blobs remain. Separate
  minor cosmetic chip.

- **Character anim-phase RECORD COUNTERS — MACHINE-verified bit-exact (player +
  companion), 2026-06-04. ⚠️ counters only — the player's DRAWN cell is NOT 1:1
  (see the CONFIRMED NOT-1:1 row).** `phase_probe.py house-walk-down-dense` over a
  45-frame window: the PLAYER anim *record* (`anim/counter/aframe/oct` at
  `&DAT_056daae8[0]`) AND the COMPANION anim (`cframe/ccnt/canim/coct`) are ALIGNED
  to the frame, incl. the walk-cycle wrap. `rngcalls` ALIGNED; raw `rng`
  SAMPLE-SKEW. **BUT this proves only the sim-side RECORD counters match — the
  user then eyeballed the aligned montage and found Recette's RENDERED walk cell
  diverges on ~18% of frames** (db054 42/50/51/59/60/68/69/84), where the record
  fields are identical yet the drawn legs are a different step. So the rendered
  cell is sourced from something the record-watch misses (ring / STATE / sub-frame)
  — see `scene1-recette-walk-cell.md`. Companion alignment is on the `{phasepin}`'d
  trace. Data: `runs/phase-probe/house-walk-down-dense/`.

See [[scene1-walk-dust]] (draw-order ground truth), [[scene1-rng-stream-parity]].
