<!--
  The ONE hand-edited status block.  tools/gen_port_ledger.py injects everything
  below the marker line verbatim into docs/STATUS.md's "Current front" section, so
  STATUS can never drift from reality.  Update THIS when the active front moves;
  keep it short (a 60-second read).  Everything else in STATUS is derived from code.

  RULES: keep ONLY open/forward-looking items here. When an item RESOLVES, move its
  story to PROGRESS.md / the findings doc in the same edit — do not let resolved arcs
  accumulate. Full pre-cleanup snapshots:
  - archive/FRONT-2026-06-09-full.md (world-map backlog, dialogue fixes, load-arc, shop-display chips)
  - archive/FRONT-2026-07-01-full.md (customer-service/haggle, title/pause/guild/prologue arcs, v3 build-out)
  - archive/FRONT-2026-08-22-full.md (evidence compiler Wave-0, GX-00→06, ST-00→05, day-2 transition arcs)
  Code-tagged PORT-DEBT lives in docs/port-debt.md (derived).
-->
<!-- FRONT:BEGIN -->
- **★ NEXT MICRO-TASK:** CC-04 unknown write-set capture (Arc 3), or BT-00 boundary-event schema.

- **▶ ACTIVE ARC 2 — AUTONOMOUS DAY-2 PLAY-THROUGH & SUBSYSTEM INTEGRATION (LANDED — ARC 2 COMPLETE).**
  - **✅ LANDED (`tests/test_day2_playthrough.c`, `tools/test_day2_autonomous.py`):** Complete autonomous Day-2 game lifecycle verified under host ASan/UBSan (10/10 tests) and Python test runner (4/4 tests).

- **▶ ACTIVE ARC 3 — PARITY EVIDENCE COMPILER & BEHAVIOR ATLAS (roadmap program).**
  - **✅ LANDED:** Wave-0 (EP-00→EP-08) complete. Save pillar (ST-00/ST-01), volatile state pillar (ST-02..06 complete — Merkle roots, mutation consumer, ST-05 mutation producer platform `tools/parity/mutation_producer.py`, ST-06 5-scene volatile state map expansion in `docs/schemas/state-volatile-v1.json`), pixels pillar, D3D8 capture census & opcode corpus (GX-00→GX-06) complete. First multi-pillar passing proof bundle on `house-pause-save-commit`.
  - **✅ LANDED (BA-00..BA-08 Behavior Atlas — `tools/atlas/`, `tools/behavior_atlas.py`, `tools/test_behavior_atlas.py` 31/31 tests passing):** Workstream BA COMPLETE. Content-addressed behavior graph (Node & Edge SHA-256 identity, SQLite store + JSON export/import, 68/68 scenarios indexed, BFS pathfinding & traversal execution engine, scene action grammars, BA-05 coverage-guided exploration scheduler, BA-06 hierarchical trace minimizer, BA-07 RNG callsite map & LCG seed solver, BA-08 multi-dimensional atlas health & risk reporter).
  - **✅ LANDED (CC-00 Call Capsules — `docs/schemas/call-capsule-v1.json`, `docs/reference/call-capsules.md`, `tools/parity/capsule.py`, `tools/test_call_capsule.py` 8/8 checks passing):** Capsule ABI & pointed-object memory model specification adopted. Content-addressed identity, cdecl/stdcall/thiscall/fastcall support, x87 bit-preservation, relocation sandboxing, memory mutation validation, and 5 canonical category fixtures (pure leaf, known globals, in-place struct mutation, RNG consumer, unsupported OS-coupled call).
  - **✅ LANDED (CC-01 Known-Write Call Capture — `tools/parity/capsule_capture.py`, `tools/capsule_capture.py`, `tools/test_capsule_capture.py` 13/13 tests passing, `tools/frida/openrecet-agent.js` `captureCallCapsule` RPC):** Snapshot-try-finally-restore lifecycle with bit-for-bit memory rollback on success and simulated crash, explicit `RacePolicy` classification, 5 canonical real engine fixtures (`boss_id_allowed`, `floor_is_checkpoint`, `rng_next15`, `records_a_spawn`, `audio_fade_compute`), and bit-exact host differential replay against ported C routines.
  - **✅ LANDED (CC-02 Host Adapters & Corpus Tests — `tools/parity/host_diff_adapter.py`, `tools/parity/adapter_gen.py`, `tools/parity/corpus_store.py`, `tools/test_capsule_adapter.py` 17/17 tests passing, `docs/schemas/fixtures/capsules/`):** Direct native C execution against `tests/build/libengine_diff.so` via ctypes marshaling, automated C struct/shim and dynamic runtime adapter synthesis from `CallCaptureSpec` (descriptor + port symbol zero boilerplate), boundary mutation cross-validation, persistent fixture store, and deliberate return/poststate/order divergence detection.
  - **✅ LANDED (CC-03 Observed Capsule Corpus Expansion — `docs/schemas/fixtures/capsules/`, `tools/parity/capsule_capture.py`, `tools/parity/host_diff_adapter.py`, `src/diff_entry.h`, `src/diff_entry.c`, `tools/test_capsule_adapter.py` 31/31 tests passing, `tools/test_capsule_capture.py` 13/13 tests passing):** Expanded canonical observed corpus to 12 targets across customer service, haggle math, item tables, equipment stats, audio predicates, stage gates, and particle systems (`haggle_decide`, `haggle_budget_ceiling`, `audio_is_one_shot_track`, `customer_service_pushback_patience`, `customer_service_budget_level_day`, `tables_item_find_slot_by_id`, `chara_equip_item_stats`), with native C shims in `libengine_diff.so`, boundary mutations, and verified caller/scenario provenance.
  - **✅ LANDED (CV-01 Offline Ghidra & Static RE Index Platform — `tools/ghidra-scripts/ExportGhidraIndex.java`, `tools/ghidra-headless.sh`, `tools/re_index.py`, `tools/coverage_atlas.py`, `tools/test_re_index.py` 16/16 tests passing):** Complete deterministic static index export platform. Headless Java post-script extracting functions, basic blocks, CFG flows, call edges, memory/data read/write xrefs, string xrefs, and switch tables; pure-Python x86-32 length/branch instruction decoder indexing all 2,620 functions, 55,343 basic blocks, 82,167 CFG flow edges, 23,436 call edges, 5,794 global xrefs, and 1,300 string xrefs in <600ms; expanded SQLite schema in `docs/re-index.sqlite` with Python APIs and CLI (`blocks`, `flows`, `switches`, `data-xrefs`, `hash`, `export-json`, `import-json`); and integrated static block verification in `coverage_atlas.py` CV-08 CFG structural calibration.
- **★ NEXT:** CC-04 unknown write-set capture (Arc 3), or BT-00 boundary-event schema.
