{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Project-shared (tracked) Claude Code settings. Personal allowlists live in settings.local.json (gitignored). Git-side hooks (co-author trailer, pre-commit ledger+tests) live in tools/git-hooks via core.hooksPath.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/tools/claude-hooks/guard-paths.sh"
          }
        ]
      }
    ]
  }
}
