"""trace_studio.server — local http viewer + JSON API + HTTP-Range video serving
(lifted from trace_studio_serve.py; the do_POST route ladder is kept as-is — the
dispatch-table refactor is Phase 4, co-designed with the SPA rewrite).
"""
